import { LabBillGeneration } from './lab-bill-generation';

describe('LabBillGeneration', () => {
  it('should create an instance', () => {
    expect(new LabBillGeneration()).toBeTruthy();
  });
});

import { PPharmacistViewModel } from './p-pharmacist-view-model';

describe('PPharmacistViewModel', () => {
  it('should create an instance', () => {
    expect(new PPharmacistViewModel()).toBeTruthy();
  });
});

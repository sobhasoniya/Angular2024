import { RAppointmentViewModel } from './r-appointment-view-model';

describe('RAppointmentViewModel', () => {
  it('should create an instance', () => {
    expect(new RAppointmentViewModel()).toBeTruthy();
  });
});

import { MedicinePrescriptions } from './medicine-prescriptions';

describe('MedicinePrescriptions', () => {
  it('should create an instance', () => {
    expect(new MedicinePrescriptions()).toBeTruthy();
  });
});
